name: Deploy to production

on:
  workflow_run:
    workflows:
      - Build and push Docker image
    types:
      - completed
  workflow_dispatch:

jobs:
  deploy:
    if: ${{ github.event_name == 'workflow_dispatch' || (github.event.workflow_run.conclusion == 'success' && github.event.workflow_run.head_branch == 'main') }}
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Configure SSH key
        env:
          SERVER_HOST: ${{ secrets.SERVER_HOST }}
          SERVER_PORT: ${{ secrets.SERVER_PORT }}
          SERVER_SSH_KEY: ${{ secrets.SERVER_SSH_KEY }}
        run: |
          set -e
          PORT="$SERVER_PORT"
          if [ -z "$PORT" ]; then PORT=22; fi
          mkdir -p ~/.ssh
          printf "%s\n" "$SERVER_SSH_KEY" | tr -d '\r' > ~/.ssh/id_rsa
          chmod 600 ~/.ssh/id_rsa
          ssh-keyscan -T 20 -p "$PORT" -H "$SERVER_HOST" >> ~/.ssh/known_hosts || true

      - name: Deploy on server
        env:
          SERVER_HOST: ${{ secrets.SERVER_HOST }}
          SERVER_PORT: ${{ secrets.SERVER_PORT }}
          SERVER_USER: ${{ secrets.SERVER_USER }}
          APP_DIR: ${{ secrets.APP_DIR }}
          APP_ENV_FILE: ${{ secrets.APP_ENV_FILE }}
          GHCR_USER: ${{ secrets.GHCR_USER }}
          GHCR_TOKEN: ${{ secrets.GHCR_TOKEN }}
          REPO_URL: https://github.com/for9653960484/govorun_ai_assist.git
          BRANCH: main
        run: |
          PORT="$SERVER_PORT"
          if [ -z "$PORT" ]; then PORT=22; fi
          if [ -z "$APP_ENV_FILE" ]; then
            echo "APP_ENV_FILE secret is empty"
            exit 1
          fi
          if [ -z "$GHCR_USER" ] || [ -z "$GHCR_TOKEN" ]; then
            echo "GHCR_USER or GHCR_TOKEN secret is empty"
            exit 1
          fi

          SSH_OPTS="-i ~/.ssh/id_rsa -o IdentitiesOnly=yes -o StrictHostKeyChecking=no"

          ssh $SSH_OPTS -p "$PORT" "$SERVER_USER@$SERVER_HOST" "mkdir -p '$APP_DIR'"
          ssh $SSH_OPTS -p "$PORT" "$SERVER_USER@$SERVER_HOST" "if [ ! -d '$APP_DIR/.git' ]; then git clone '$REPO_URL' '$APP_DIR'; fi"
          ssh $SSH_OPTS -p "$PORT" "$SERVER_USER@$SERVER_HOST" "cd '$APP_DIR' && git fetch origin '$BRANCH' && git checkout '$BRANCH' && git reset --hard 'origin/$BRANCH'"

          printf "%s\n" "$APP_ENV_FILE" | ssh $SSH_OPTS -p "$PORT" "$SERVER_USER@$SERVER_HOST" "cat > '$APP_DIR/.env'"

          ssh $SSH_OPTS -p "$PORT" "$SERVER_USER@$SERVER_HOST" "echo '$GHCR_TOKEN' | docker login ghcr.io -u '$GHCR_USER' --password-stdin"
          ssh $SSH_OPTS -p "$PORT" "$SERVER_USER@$SERVER_HOST" "cd '$APP_DIR' && docker compose -f docker-compose.prod.yml pull"
          ssh $SSH_OPTS -p "$PORT" "$SERVER_USER@$SERVER_HOST" "cd '$APP_DIR' && docker compose -f docker-compose.prod.yml up -d --remove-orphans"
          ssh $SSH_OPTS -p "$PORT" "$SERVER_USER@$SERVER_HOST" "docker image prune -f"
